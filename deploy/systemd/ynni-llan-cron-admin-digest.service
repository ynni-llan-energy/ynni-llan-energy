[Unit]
Description=Ynni Llan weekly admin digest cron
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
EnvironmentFile=/opt/ynni-llan-energy/.env
ExecStart=/usr/bin/curl -fsS -X GET -H "Authorization: Bearer ${CRON_SECRET}" http://localhost:3000/api/cron/admin-digest
